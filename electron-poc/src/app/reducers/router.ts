import { RouterReducerState, BaseRouterStoreState, routerReducer } from '@ngrx/router-store';

export const initialState: RouterReducerState<BaseRouterStoreState> = undefined;

export const reducer = routerReducer;
